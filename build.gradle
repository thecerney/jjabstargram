buildscript {
    ext {
        springBootVersion = '2.3.5.RELEASE'
        springDependencyVersion = '1.0.10.RELEASE'
    }
    repositories {
        jcenter()
    }
}

allprojects {
    group = 'com.nicekkong'
    version = '0.0.1-SNAPSHOT'

    repositories {
        jcenter()
    }
}
